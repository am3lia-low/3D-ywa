# StoryWorld low-poly models

These GLB files are original project assets generated from geometric primitives by `scripts/generate-models.mjs`. They contain no third-party meshes, textures or animations.

Regenerate them with:

```bash
pnpm models:generate
```

Every model is normalized to a unit bounding box. The spatial runtime applies semantic dimensions from the asset registry at render time.

