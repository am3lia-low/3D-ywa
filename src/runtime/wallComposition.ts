/** Shared physical clearances for trim and wall-mounted presentation assets. */
export const WALL_COMPOSITION = Object.freeze({
  wallSurfaceInset: 0.06,
  trimCenterInset: 0.07,
  trimDepth: 0.08,
  archiveTrimCenterInset: 0.065,
  archiveTrimDepth: 0.12,
  // Furniture meets the face of the skirting instead of intersecting it.
  dressingClearance: 0.145,
});
